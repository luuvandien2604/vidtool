[project]
name = "videotool"
version = "0.1.0"
description = "AI Editorial Director - automated documentary video production system"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
